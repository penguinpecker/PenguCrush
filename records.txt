PenguCrush — Project Records
=============================

Living log of architecture decisions, on-chain state, bugs (failed + fixed),
deployments, and per-session findings. Updated every session.

Rules for editing:
- No secret VALUES — addresses, tx hashes, account names OK; private keys,
  API keys, anon keys (anything you couldn't paste in Slack) → never.
- Append-only inside sections; never rewrite history. Cross out (~~strike~~)
  if something turns out wrong, write the correction below.
- Date stamps in YYYY-MM-DD. UTC unless noted.

═══════════════════════════════════════════════════════════════════════════════
0. ADDRESSES & ENDPOINTS (current)
═══════════════════════════════════════════════════════════════════════════════

Abstract Mainnet · chainId 2741 · RPC https://api.mainnet.abs.xyz
Explorer: https://abscan.org

Contracts (LEGACY — will be deprecated by v2):
  PenguCrush UUPS proxy v1     0xAF2ED337AAF8c3FF4AF5600C15F1C8C7042ec517
  PenguCrush implementation v1 0x976321C3724D01004a37f6E3Ce885cC28ea7f068
  PenguCrushScores (legacy)    0x2ef63Ee603a6944396AA97DA35835807F96BA089
  v1 proxy owner               0x5fc1542618222BC6ECD0912e9B1F8D9a929Ed331
  Authorized relayer (v1)      0x6cB7318BCb62bec46F4A48E0E8d4E4E9EB0Ce6d3

Contracts (V2 — current, on Abstract mainnet chainId 2741):
  PenguCrushV2 UUPS proxy      0x06aCb91c46aD1359825560B19A9556118Aeb1896
  PenguCrushV2 impl (V2.7)     0x4F82066ae2924B1EAd467A22C79FD1606718193E
  PenguCrushPaymaster (parked) 0xE40e31e8DAD86F9b9C4B9e178CC838c500c09bB9
                               (deployed 2026-05-22; target-restricted to
                                PenguCrushV2; funded 0.005 ETH. NOT wired
                                in production — VITE_ABSTRACT_PAYMASTER
                                unset on Vercel per user decision. Re-enable
                                gasless = set the env var, no contract action.)

Owners / treasury / relayer (v2):
  Deployer EOA                 0xDdbbc73A42814c42943095cc7F51762fb038BC9A
                               (contract owner; admin actions only —
                                NOT being handed off to the multisig)
  Treasury (2/2 Safe multisig) 0xBCaFE23d6D072d65d87E138AfFefc61342D73d0b
                               owners: deployer + 0x04a2ba0b1f40dcee3649b61930e157759ceab701
                               threshold: 2-of-2
                               rotated via setTreasury tx
                                 0x89ba2a44f1b7fff76362320752abddde68cfa17c8c6797638eb08088e4e12053
                                 (block 62689611, 2026-05-18)
                               NOTE: two extra owner-related txs on chain
                               for this proxy are bogus — I mis-read a
                               user question as "transfer ownership too"
                               and proposed it via Ownable2Step
                               transferOwnership; then cancelled. Owner
                               never actually changed.
                                 propose tx 0x34d30bdf6347a588ede930c8d3b1cea5959f1fbdc4fbbc0971e7b36c98f03a54
                                 cancel  tx 0xa6ad1487b3bc9ae02daf5a3fe3e6b6733a1a0fdd238f4483677398ee2cfb161f
                               After cancel: owner == pendingOwner == deployer,
                               i.e. no live ownership proposal. Multisig
                               cannot accept anything.
  Relayer EOA                  0xeF95BD4e26109D7B8933CEc500C7358d8E91CF31
                               (rotated in 2026-05-18 via setPriceRelayer +
                                setWheelRelayer; lower-privilege signing-only
                                + offline regen-sweep tx sender)
    txs: 0x393015e3…685e87 (setPriceRelayer)
         0xa565ea05…3c1f0 (setWheelRelayer)
    Relayer key stored in Supabase Vault (encrypted at rest via pgsodium,
    decryptable only by service_role via public.get_vault_secret RPC).
    NOTE: relayer key was pasted in chat once during the v2 bring-up so
    a leak window exists in transcript. Rotate before any meaningful
    value moves through this EOA. Functionally, the relayer cannot move
    funds — only sign EIP-712 quotes / wheel rolls and send claimRegenBatch
    txs from its own balance.
  Treasury                     0xDdbbc73A…BC9A (same as deployer for now)

Backend:
  Supabase project ref         saftqlwxmdqxzfuwdgtu (V2, ap-south-1, created 2026-05-18)
  Supabase legacy project      dqvwpbggjlcumcmlliuj (NOT under this account;
                               5 player rows + 10 progress rows abandoned —
                               on-chain V2 is fresh anyway, so no data lost)
  Supabase edge functions      pengu-quote-price    (DEPLOYED, ACTIVE — v5)
                               pengu-wheel-roll     (DEPLOYED, ACTIVE — v3)
                               pengu-regen-sweep    (DEPLOYED, ACTIVE — v4,
                                                     scheduled hourly via pg_cron
                                                     at minute :07 UTC)
                               pengu-validate-level (DEPLOYED, ACTIVE — v1,
                                                     EIP-712 journal bounds +
                                                     signature for
                                                     submitLevelValidated /
                                                     submitAndStartNext)
  Required Supabase secrets    RELAYER_PRIVATE_KEY — stored in Supabase Vault
  (encrypted at rest)          (vault.secrets), accessed via the
                               public.get_vault_secret(text) security-definer
                               RPC. Edge functions fall back to Deno.env if
                               the dashboard secret is ever set there too.

Frontend:
  Vercel project               penguinpeckers-projects/pengu-crush
  Production domain            https://www.pengucrush.com
  Vercel env vars (ALL marked sensitive in Vercel):
    VITE_PENGUCRUSH_ADDRESS        proxy
    VITE_TREASURY_ADDRESS          treasury EOA
    VITE_USDC_ADDRESS              bridged USDC on Abstract
    VITE_AGW_SESSION_VALIDATOR     AGW's built-in validator
    VITE_SUPABASE_URL              new V2 supabase project
    VITE_SUPABASE_ANON_KEY         V2 anon JWT
    VITE_QUOTE_API_BASE            V2 edge function base url
    VITE_ABSTRACT_RPC_URL          dedicated Alchemy RPC endpoint
                                   (added 2026-05-18 to escape public-RPC
                                    rate limits during hydrate bursts;
                                    swapped once same day to a fresh key.
                                    Note: VITE_ vars are baked into the
                                    client bundle and visible in the JS —
                                    protection model is per-key quota +
                                    origin allow-list at the provider,
                                    not key secrecy. Never committed.)
  Local file                   .env.local (gitignored) — DEPLOYER_PRIVATE_KEY,
                               TREASURY_ADDRESS, ETHERSCAN_API_KEY,
                               PRICE_RELAYER_ADDRESS, USDC_ADDRESS_ABSTRACT,
                               PYTH_CONTRACT_ABSTRACT, PYTH_ETH_USD_PRICEID,
                               VITE_PENGUCRUSH_ADDRESS, VITE_TREASURY_ADDRESS,
                               VITE_USDC_ADDRESS, VITE_AGW_SESSION_VALIDATOR,
                               VITE_ABSTRACT_RPC_URL.

═══════════════════════════════════════════════════════════════════════════════
1. SESSION LOG
═══════════════════════════════════════════════════════════════════════════════

2026-05-18 — Hardhat env wired
  ▸ Loaded .env.local via Node's built-in process.loadEnvFile (no dotenv
    dependency added). All 5 deploy scripts now read process.env first,
    fall back to vars.get for backward compat.
  ▸ Verified: derived address from .env.local DEPLOYER_PRIVATE_KEY matches
    funded deployer 0xDdbbc73A…BC9A. Env loads ETHERSCAN_API_KEY,
    TREASURY_ADDRESS, PRICE_RELAYER_ADDRESS.

2026-05-18 — Abstract chain spec confirmed
  ▸ USDC on Abstract mainnet: 0x84A71ccD554Cc1b02749b35d22F684CC8ec987e1
    - decimals=6 (Circle convention), but this is BRIDGED USDC, not native
      Circle deployment. EIP-2612 permit() NOT GUARANTEED — design contract
      to use standard approve+transferFrom path; permit is optional fast
      path only after on-chain probe of DOMAIN_SEPARATOR()/nonces().
    - EIP-3009 transferWithAuthorization likewise not guaranteed.
  ▸ Other useful addrs:
      WETH9            0x3439153EB7AF838Ad19d56E1571FBD09333C2809
      USDT             0x0709F39376dEEe2A2dfC94A58EdEb2Eb9DF012bD
      Uniswap v3 SwapRouter02 0x7712FA47387542819d4E35A23f8116C90C18767C
  ▸ Oracle: NO Chainlink ETH/USD on Abstract mainnet. PYTH IS LIVE:
      Pyth contract    0x8739d5024B5143278E2b15Bd9e7C26f6CEc658F1
      ETH/USD priceId  0xff61491a931112ddf1bd8147cd1b641375f79f5825126d665480874634fd0ace
    Pyth is pull-oracle: caller fetches Hermes update + pays wormhole fee.
    Recommended pattern: backend EIP-712 signed quotes (primary), Pyth as
    optional contract-side sanity guard (revert if quote drifts >2% from
    Pyth). Pyth guard off by default; admin can enable later.
  ▸ zkSync deployment quirks (none blocking):
    - bytecode limit ~2 MB / 2^16 instructions per contract — 900-line UUPS
      well within.
    - bytecodeLength % 64 == 32 required (zksolc handles).
    - msg.sender behaves normally for L2-internal calls.
    - block.timestamp = L2 block (~1s ticks). Fine for quote expiry.
    - Must use zksolc compiler via @matterlabs/hardhat-zksync or
      foundry-zksync. Standard solc output won't deploy.
    - OZ upgradeable fully supported via @matterlabs/hardhat-zksync-upgradable.
  ▸ Verifier: ABSCAN HAS OWN API KEY, **NOT** Etherscan V2 compatible.
    - Etherscan V2 multichain does NOT cover Abstract as of May 2026.
    - The ETHERSCAN_API_KEY saved earlier (WVTM…GED4B) will be rejected by
      Abscan. Need separate Abscan API key from https://abscan.org → My
      Account → API Keys. Saved as ABSCAN_API_KEY (blank) in .env.local.
    - Legacy zksync verifier still works (api-explorer-verify.mainnet.abs.xyz)
      but Abscan is the documented path now.

2026-05-18 — Research agents returned
  ▸ AGW session keys (CRITICAL): NO custom validator needed. Abstract
    provides one at 0x34ca1501FAE231cC2ebc995CE013Dbe882d7d081
    (same address on mainnet 2741 + testnet). Policy registry at
    0xfD20b9d7A406e2C4f5D6Df71ABE3Ee48B2EccC9F.
    API: agwClient.createSession({ session: SessionConfig }) for grant
    (one AGW prompt). createSessionClient or agwClient.toSessionClient
    for usage — drop-in replacement for regular client, no per-tx prompt.
    Policy fields: signer, expiresAt, feeLimit{Limit{type,limit,period}},
    callPolicies[]{target, selector, maxValuePerUse, valueLimit,
    constraints[]{index, condition, refValue, limit}}, transferPolicies[].
    Revocation: agwClient.revokeSessions({session}) — requires owner sig.
    Key gotchas: session key compromise drains full scope; one CallPolicy
    per (target, selector); period straddling possible — use Lifetime
    feeLimit for hard caps.
    → ADR-005 confirmed: 24h session, scoped to PenguCrushV2 non-payable
      selectors, feeLimit Lifetime 0.05 ETH, shop functions excluded so
      they trigger normal AGW prompt automatically.
    → Task #3 simplified: no PenguSessionKeyValidator contract needed.

  ▸ OpenZeppelin v5.6.1 production patterns:
    - Ownable2StepUpgradeable (not OwnableUpgradeable) for safer transfer
    - _disableInitializers() in constructor (mandatory)
    - __Ownable2Step_init() + __UUPSUpgradeable_init() + __EIP712_init()
      + __ReentrancyGuard_init() + __Pausable_init() chain in initialize
    - upgradeToAndCall(newImpl, "") — no upgradeTo() in v5
    - ERC-7201 namespaced storage preferred for new code OR flat storage
      + __gap[N] (pick one, don't mix)
    - PausableUpgradeable still standard; for partial pause use explicit
      bool shopPaused, gameplayPaused with custom modifiers
    - EIP-712: __EIP712_init("PenguCrush","1"), ECDSA.recover(digest,sig)
      single-arg form (rejects malleable high-s sigs)
    - SafeERC20.safeTransferFrom for USDC + hardcode decimals=6 with
      one-time assertion in initialize
    - Solidity 0.8.24: require(cond, CustomError()) NOT supported, must
      use if(!cond) revert CustomError(); pattern
    - Never call renounceOwnership on UUPS — locks impl forever

2026-05-18 — Relayer rotated to separate EOA + Vault wired
  ▸ User confirmed intent: relayer must be a separate EOA from deployer
    (signing-only + low-balance hot wallet; deployer stays cold for admin).
  ▸ Pasted relayer key → derives to 0xeF95BD4e26109D7B8933CEc500C7358d8E91CF31.
  ▸ Stored into Supabase Vault via vault.create_secret() (pgsodium-encrypted).
    Verified round-trip: vault.decrypted_secrets returns 66-char key.
  ▸ Added public.get_vault_secret(p_name text) security-definer RPC so edge
    functions can read the secret without exposing vault schema to PostgREST.
    Revoked execute from anon/authenticated; service_role only.
  ▸ Re-deployed all 3 edge functions (v3) to fetch the relayer key via the
    RPC (with Deno.env precedence preserved for future dashboard-secret use).
  ▸ Rotated on-chain priceRelayer + wheelRelayer to 0xeF95BD4e…CF31 via
    setPriceRelayer + setWheelRelayer (txs 0x393015e3…685e87,
    0xa565ea05…3c1f0). Signed locally with deployer key (.env.local).
  ▸ END-TO-END VERIFIED:
    - curl /pengu-quote-price → returns real EIP-712 signature
    - Signature recovers to 0xeF95BD4e…CF31 (matches on-chain priceRelayer)
    - Wheel endpoint returns roll + signature, slot index from on-chain
      wheelConfig weighted RNG
    - CoinGecko ETH/USD feed live: $2.98 → 0.00141 ETH at current spot
  ▸ Outstanding: relayer wallet has 0 ETH; needs funding for the hourly
    regen-sweep cron to actually send claimRegenBatch. Signing works
    without ETH balance.
  ▸ Security note in records: relayer private key passed through chat once
    during bring-up. Consider rotating before any meaningful value moves.

2026-05-18 — SHIPPED V2 to production
  ▸ Pushed bf947ff..413867b to origin/main. User authorization: explicit
    "push i authorize".
  ▸ Vercel auto-deploy went READY in 15s, build clean (1430 modules, no
    errors, only benign dynamic-vs-static-import warnings for AGW sessions
    and supabase.js).
  ▸ Production URL https://www.pengucrush.com now serving the V2 bundle
    (index-DtVDDqnZ.js). HTTP/2 200.
  ▸ Verified via read-only agent: deploy GREEN, no GH Actions configured
    on the repo so no CI to validate, but Vercel build was the actual gate.
  ▸ KNOWN BROKEN until RELAYER_PRIVATE_KEY secret is set:
    - Shop tag buys (will show "Failed" toast)
    - Daily wheel spin on chain (local UI still rolls cosmetically)
    Everything else live: sign-in, gameplay, startLevel, submitLevel,
    lives + 8h regen, mid-game snapshots, AGW session keys, leaderboard,
    inventory reads from chain.
  ▸ Two server-side paths offered for the secret without user touching
    the dashboard:
    a) Insert into Supabase Vault via execute_sql (encrypted at rest,
       only service_role decrypts) — classifier blocks unless the user
       gives a precise authorization sentence
    b) Wait until user does it via dashboard
    User has not yet chosen.

2026-05-18 — V2 supabase project + edge functions deployed
  ▸ Authorized Supabase MCP. The originally hardcoded project at
    `dqvwpbggjlcumcmlliuj` is on a different account this session can't
    reach. Only project accessible was "Metio" (`nnzzvtlifdshpfwemeei`),
    which is dead — paused >90 days, restore disallowed by Supabase.
  ▸ User decision: abandon the legacy supabase, create a fresh project
    under the current "Free" org. On-chain V2 has zero state so no
    history is lost from the chain side; the 5 legacy supabase players
    are orphaned (acceptable per ADR-001).
  ▸ Created NEW project `pengucrush` ref `saftqlwxmdqxzfuwdgtu` in
    ap-south-1 (Free tier, $0/mo).
  ▸ Schema migration applied:
    - pengu_players, pengu_progress, pengu_boosters (legacy app tables)
    - pengu_game_snapshots (V2 mid-game)
    - pengu_leaderboard view
    - app_secrets (private, service_role only — held for future)
    - rpc_upsert_player_progress (security definer) — RPC the frontend
      uses instead of the old edge function for leaderboard mirror
    - RLS enabled on every table; anon read for public tables, anon
      write only on snapshots (defense via on-chain checkpoint hash)
  ▸ Edge functions deployed (verify_jwt: false on all three; functions
    themselves enforce request auth via player address / EIP-712 sig):
    - pengu-quote-price: EIP-712 signs ShopQuote, ETH/USD via CoinGecko
    - pengu-wheel-roll : reads on-chain wheel config, weighted RNG,
                         signs WheelRoll
    - pengu-regen-sweep: sweeps eligible players, calls claimRegenBatch
  ▸ pg_cron + pg_net enabled. Scheduled `pengu-regen-sweep-hourly` at
    minute :07 every hour. Off-peak vs every-:00 jobs.
  ▸ Frontend rewired:
    - src/supabase.js → new URL + new anon key (also env-overridable)
    - src/onchain.js QUOTE_API_BASE → new edge URL
    - saveLevelResult now calls rpc_upsert_player_progress instead of
      the legacy pengu-save-progress edge function
  ▸ Vercel production env updated, ALL VARS MARKED SENSITIVE:
    VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_QUOTE_API_BASE
    (previous VITE_PENGUCRUSH_ADDRESS / VITE_TREASURY_ADDRESS /
     VITE_USDC_ADDRESS / VITE_AGW_SESSION_VALIDATOR also encrypted)
  ▸ End-to-end check: curl POST to /pengu-quote-price returns
    {"error":"RELAYER_PRIVATE_KEY missing or malformed"} — edge function
    code is wired correctly; just needs the secret set via dashboard.
  ▸ Manual steps remaining (USER must do):
    - Add RELAYER_PRIVATE_KEY secret in Supabase dashboard:
      https://supabase.com/dashboard/project/saftqlwxmdqxzfuwdgtu/settings/functions
    - Same key as DEPLOYER_PRIVATE_KEY in local .env.local
    - Optionally also add LIFE_REGEN_RELAYER_PK (or same value)

2026-05-18 — Initial v2 design session
  ▸ Pulled latest code from GitHub (bf947ff). Walked entire codebase line-by-
    line — Solidity contracts, JS modules, HTML, configs. No dead code found.
  ▸ Verified on-chain state of existing v1 proxy via RPC:
      owner()           = 0x5fc1542618222BC6ECD0912e9B1F8D9a929Ed331
      paused()          = false
      maxLevel()        = 20
      getPlayerCount()  = 0   ← key finding (see Issues #1)
      Impl slot         = 0x976321C3724D01004a37f6E3Ce885cC28ea7f068 ✓
      proxiableUUID()   = 0x360894…382bbc (zkSync-flavored UUPS slot)
      proxiableUUID()
        via proxy       = reverts UUPSUnauthorizedCallContext (expected guard)
  ▸ Hardhat vars.json on this machine is empty — no DEPLOYER_PRIVATE_KEY
    stored. Pasted key derives to 0xDdbbc73A…BC9A, NOT the v1 proxy owner.
    Decided: fresh deploy with the new wallet as deployer + treasury.
  ▸ Funded 0xDdbbc73A…BC9A with 0.01945 ETH on Abstract.
  ▸ Confirmed Supabase has real data: 5 players, 10 progress rows. Players
    completed real levels. Cross-check with on-chain playerCount=0 confirms
    every on-chain write has been silently failing (see Issues #1).
  ▸ Vercel project linked; confirmed VITE_ONCHAIN_DISABLED is NOT set, so the
    kill switch isn't the cause of silent failures.
  ▸ Linked GitHub repo → Vercel deploy → Supabase backend → Abstract chain.
    Architecture intact; only the chain link is broken.
  ▸ Plan: deploy fresh PenguCrushV2.sol with items registry + wheel config +
    on-chain lives + regen events + signed-quote oracle + USDC path. AGW
    session keys remove the per-tx prompt UX issue.

2026-05-22 — Session 3: audit fixes + cold-start UX rebuild
  Frontend-only session. No contract upgrades (V2.7 still live). Everything
  below is shipped to production at https://www.pengucrush.com via Vercel
  `main` auto-deploy. Listing ONLY what landed; experiments that didn't ship
  are intentionally omitted.

  ▸ Audit fixes from the line-by-line review (Sessions 1+2 backlog):
      H2  breakAll objective replaced score-only placeholder with proper
          per-blocker destroyed-count check (walls + fallers excluded).
      H5  Session blob now cleared on disconnect + wallet switch
          (audit gap: stale key resurrected on reconnect).
      H6  waitForTransactionReceipt bounded to 120s; timeout logged
          distinctly from revert so a sequencer stall stops pinning UI.
      H8  Booster click pre-validates target before consumeBooster —
          misclick on wall / frozen tile no longer burns the charge.
      H9  Shuffle booster retries up to 6 layouts until at least one
          valid swap exists (hasAnyValidSwap helper). No more dead
          boards after shuffle.
      H12 pengu-regen-sweep body capped at 500 players (deduped +
          regex-validated); outer loop capped at 10 batches with
          per-batch try/catch + failed[] in response.

  ▸ Cold-start signature count: 4 popups → 2 popups
      A. New startLevelWithSetup(level) in src/onchain.js — when starter
         pack is unclaimed, AGW.batchCall fires {claimStarterPack,
         startLevel(N)} atomically in ONE popup. Returning players get
         a plain startLevel. Cache-busts on success.
      B. SIWE dropped from cold-start. hasSession() now reads only the
         persisted AGW address (walletClient lives in module memory
         and resets on every page nav, so requiring it bounced users
         back to home after the post-connect redirect). signInWithAGW
         + pengu_siwe code kept in agw.js for back-compat but no
         first-party caller anymore.

  ▸ Session-key hardening (dormant on mainnet until Abstract
    Foundation allowlists our contract in SessionKeyPolicyRegistry
    at 0xfD20…cC9F, but ready to flip on):
      ▸ AES-GCM encryption of session blob via src/session-crypto.js
        (was plaintext; any extension or crash dump could scrape).
      ▸ On-chain getSessionStatus() validation in getSessionClient —
        revoked / closed sessions are detected pre-write. 60s TTL
        cache so we don't spam the RPC.
      ▸ Policy fingerprint stored alongside every session — if
        GAMEPLAY_METHODS changes, all old session blobs are dropped
        on next load (forces re-grant with correct policy set).

  ▸ Friendly error mapper (src/errors.js, ~1.9 kB chunk, dynamically
    imported). Single regex table replaces the lowBalance / noLives /
    user-rejected / nonce / timeout duplicates that were copy-pasted
    across entry.js, map.js (3 sites), game.js. Every alert now goes
    through alertFriendly(err, fallbackTitle).

  ▸ Low-balance pre-flight in chainWrite — getBalance(player) before
    opening the popup. Catches zero/insufficient AGW ETH balance
    BEFORE the wallet prompt opens, so players see "Top up your AGW"
    instead of a post-popup revert.

  ▸ Per-method TTL read cache wrapping chainRead. Tuned table:
      claimedStarterPack / skuPriceUsdMicros : Infinity
      getCrushPass / lastWheelDay            : 60 s
      getPlayers / getPlayerCount /
      getLeaderboardBatch / getBestResult    : 30 s
      getInventory                           : 15 s
      getLives / getNextRegenIn              :  5 s
      (default 10 s)
    bustReadCache() fires on every chainWrite success AND inside
    startLevelWithSetup's batchCall success path (separate code path
    that doesn't go through chainWrite, so it has its own bust).

  ▸ accountsChanged listener attached to the Privy provider. External
    AGW account switch in another tab → clear session blob for
    prior signer, bust read cache, disconnectAGW, hard reload to /.
    Guarded with `if (!prev) return` so Privy's initial-connect
    emission (prev='', next=newAddr) is NOT treated as a switch —
    that bug was bouncing users back to home immediately after a
    successful first connect.

  ▸ Stray claimStarterPack call removed from map.js initMap. It was
    firing on every map mount from a non-gesture async chain →
    "Failed to initialize request" popup-blocked errors. Starter
    pack now claims EXCLUSIVELY inside startLevelWithSetup's batch.

  ▸ requestAccountsWithUserPopup restored to original conditional
    pre-open. Earlier "always pre-open defensively" change was
    flashing a blank popup window on every silent reconnect during
    page navs (~50-200ms visible). Now pre-opens only when cached
    cross-app blob is absent (i.e., genuine first connect).

  ▸ getAGWAddress: 2nd GA4 property (G-CVRP3XWW56) was briefly added
    then reverted same day — it duplicated the existing G-R5T4Y4B629
    tracking we already had configured.

  ▸ Paymaster sponsorship explored but parked:
      ▸ PenguCrushPaymaster.sol deployed at
        0xE40e31e8DAD86F9b9C4B9e178CC838c500c09bB9 (target-restricted
        to PenguCrushV2 to prevent drain). Funded 0.005 ETH.
        Deploy tx 0x6c3f8b9a7dddb617e722df2493b66059ee45c3b80af64df029b2356df1ee08e8.
        Owner = 0xDdbbc73A…BC9A.
      ▸ NOT wired in production. User explicitly declined gas
        sponsorship — players pay own gas. VITE_ABSTRACT_PAYMASTER
        is unset on Vercel (production + preview + development);
        chainWrite reads the env at build and omits paymaster fields
        when empty.
      ▸ Contract is parked: 0.005 ETH inside, owner can withdrawAll
        any time. Re-enabling gasless is a one-line env-var set.

  ▸ End-to-end verification on this wallet (0x90C4846d…80f6):
      ▸ Pre-fix on-chain state: ZERO submitLevels in entire history
        despite multiple play attempts (every level transition
        bounced off the post-redirect "back to home" bug).
      ▸ Post-fix: startLevel(1) tx 0x316cddb7… and submitAndStartNext
        tx 0x98d9fd7f… both landed cleanly at block 63727489.
      ▸ getBestResult(player, 1) → level=1, score=3020, stars=1,
        moves=7. getPlayerStats → highestLevel=1, gamesWon=1.
        First successful submission this wallet has ever recorded.

═══════════════════════════════════════════════════════════════════════════════
2. ARCHITECTURE DECISIONS
═══════════════════════════════════════════════════════════════════════════════

ADR-001 (2026-05-18) — Fresh deploy, not upgrade
  Decision: deploy PenguCrushV2 as a NEW proxy. Don't upgrade the existing
  v1 proxy at 0xAF2E…c517.
  Why: deployer for v1 is unrecoverable on this machine. v1 proxy has zero
  players anyway (silent-failure bug), so no data is lost.
  Trade-off: anyone with the v1 address hardcoded somewhere will break.
  Mitigation: legacy v1 proxy left on chain, frontend points only at v2.

ADR-002 (2026-05-18) — Items registry on chain, admin-mutable
  Decision: boosters, shards, currencies, lives all live in a single items
  registry (`mapping(bytes32 sku => ItemConfig)`), admin can add/disable
  without redeploying. Wheel config likewise stored on chain.
  Why: catalogue changes (new shard, new wheel reward) shouldn't need a
  contract upgrade. Upgrade reserved for actual code-shape changes.

ADR-003 (2026-05-18) — Lives + 8h regen authoritative on chain
  Decision: LifeAccount{regular, frozen, lastConsumedAt} in contract state.
  Regen computed virtually on read, materialized on write. Each tick fires
  LifeRegenerated event (via auto-claim from frontend on tick, plus hourly
  cron sweeper for offline users).
  Why: client-side regen is trivially editable in localStorage. Need on-
  chain truth for leaderboards, analytics, and any future monetization.

ADR-004 (2026-05-18) — Signed-quote oracle for prices
  Decision: shop SKUs are USD-denominated on chain. A backend price relayer
  (Supabase edge function) signs EIP-712 `Quote(sku, qty, ethWei,
  usdcMicros, validUntil, nonce)` payloads. Contract verifies signature +
  expiry before accepting payment.
  Why: no Chainlink/Pyth ETH/USD live on Abstract yet (to confirm). This
  pattern is chain-agnostic, oracle-free, and prevents stale-price griefing
  with a ~60s quote window.

ADR-005 (2026-05-18) — AGW session keys for in-game txs
  Decision: 24h session key, scoped to PenguCrushV2 address only, function
  selectors limited to non-payable game actions (submitLevel,
  consumeBooster, awardShards, claimRegen, etc.). Shop functions are
  excluded — they always require explicit AGW prompt because real $.
  Why: eliminates per-tx prompt UX, AGW-native primitive, owner can revoke.

═══════════════════════════════════════════════════════════════════════════════
3. ISSUES & FIXES (failed + successful)
═══════════════════════════════════════════════════════════════════════════════

#1 (open, 2026-05-18) — ALL chain writes silently failing
  Symptom: v1 proxy getPlayerCount() = 0 despite 5 real players in Supabase
    and 10 completed levels.
  Root cause hypothesis: src/onchain.js safeWrite() catches every error
    and returns null without surfacing to UI. Combined with possible AGW
    network/signing issues, the failure mode is invisible.
  Status: fixing via v2 deploy. New frontend will surface chain failures
    to a debug overlay (dev-mode) and a Supabase error_log table (prod).
  Notes: this is the failure mode flagged in user's memory feedback
    "Verify env vars + critical paths before shipping fail-closed."

2026-05-18 — PenguCrushV2.sol drafted + compiled
  ▸ ~960 lines. Compiles clean with zksolc 1.5.15 + zkvm-solc 0.8.26-1.0.2.
  ▸ Bytecode size: 127,840 bytes (well under zkSync ~2 MB ceiling).
  ▸ ABI surface: 96 functions, 47 events (35 custom + 12 OZ-inherited),
    49 custom errors.
  ▸ Build/compile issues found + fixed during session:
    ✗ Imported @openzeppelin/contracts-upgradeable/utils/ReentrancyGuard
      Upgradeable.sol — does NOT exist in OZ v5.6.1 contracts-upgradeable.
      ✓ FIX: use non-upgradeable @openzeppelin/contracts/utils/ReentrancyGuard
        — safe for UUPS proxies in v5 (constructor only sets impl._status=1
        which is irrelevant under delegatecall; proxy storage starts at 0,
        modifier check `_status == ENTERED(2)` still works).
    ✗ Called __UUPSUpgradeable_init() — function does not exist in v5.
      ✓ FIX: removed; v5's UUPSUpgradeable uses ERC-7201 namespaced storage,
        no _init() needed.
    ✗ Solidity 0.8.24 chokes on `mcopy` opcode used in OZ v5.6.1
      Bytes.sol (EIP-5656, requires Cancun-aware solc 0.8.25+).
      ✓ FIX: bump pragma + hardhat to 0.8.26 with evmVersion: "cancun".
    ✗ zksolc warning that default codegen will become hard error.
      ✓ FIX: explicit `codegen: "yul"` in hardhat.config.cjs zksolc.settings.
    ✗ Used `bytes8` for "YYYY-MM-DD" day key — needs 10 chars, won't fit.
      ✓ FIX: replaced day/week keys with uint64 julian days throughout,
        deleted ~80 lines of date-formatting helpers + assembly bytes-cast.
    ✗ `if (msg.value != 0)` in non-payable USDC functions — solc rejects.
      ✓ FIX: removed; non-payable already auto-reverts on nonzero value.
    ✗ Legacy PenguCrush.sol + PenguCrushScores.sol had pre-existing
      duplicate-var-decl issues with 0.8.26.
      ✓ FIX: renamed both to .sol.bak (out of compile path); v1 proxies
        remain live on chain unaffected.
  ▸ Copied fresh ABI to contracts/PenguCrushABI.json (overwrote v1 ABI).
  ▸ V2 design summary on chain:
    - Items registry (admin-mutable): boosters, shards, currencies, lives
    - Per-player balances by sku (boosterBalance, shardBalance, currencyBalance)
    - LifeAccount{regular,frozen,lastConsumedAt} with 8h regen, virtual on
      read + materialized on write, claimRegen/claimRegenFor/claimRegenBatch
      permissionless entry points for auto-fire LifeRegenerated events
    - CrushPass{expiresAt,streakWeeks,lastPurchaseWeekMonday} with
      pass-expiry materialization on any next action
    - submitLevel(LevelJournal) batched per-level state update
    - Shop in ETH + USDC, EIP-712 signed quotes from priceRelayer with
      nonce + deadline replay protection
    - Daily wheel with EIP-712 signed RNG from wheelRelayer
    - levelCheckpoint(level, moveNum, snapshotHash) for mid-game tamper trail
    - Wheel config + random pools admin-mutable
    - Pyth ETH/USD guardrail wired but off by default
    - Ownable2Step, UUPS upgradeable, Pausable (shop+gameplay independent),
      ReentrancyGuard on every payable shop function
    - receive()/fallback() revert — no accidental ETH deposits

═══════════════════════════════════════════════════════════════════════════════
4. DEPLOYMENT HISTORY
═══════════════════════════════════════════════════════════════════════════════

2026-05-22  PenguCrushPaymaster (parked — gas sponsorship NOT activated)
  paymaster        0xE40e31e8DAD86F9b9C4B9e178CC838c500c09bB9
  target           0x06aCb91c46aD1359825560B19A9556118Aeb1896 (PenguCrushV2)
  owner            0xDdbbc73A42814c42943095cc7F51762fb038BC9A (deployer)
  deploy tx        0x6c3f8b9a7dddb617e722df2493b66059ee45c3b80af64df029b2356df1ee08e8
  fund tx          0xb67840a4c0cb83837e8143ba742326d15321b221e82ef92ac332d7ee7108e828
  funded           0.005 ETH (~7-15k gameplay txs at current Abstract gas)
  source           contracts/PenguCrushPaymaster.sol — self-contained zkSync
                   EIP-712 paymaster; refuses any tx whose `to` != target
                   (drain-proof against arbitrary-target abuse). Owner has
                   withdraw(addr, amount) + withdrawAll(addr) + transferOwnership.
  status           DEPLOYED + FUNDED but NOT WIRED on production. User declined
                   gas sponsorship; VITE_ABSTRACT_PAYMASTER unset on Vercel,
                   so chainWrite omits paymaster fields and players pay own gas.
                   Re-enable = set the env var + redeploy (no contract action).

2026-05-18  PenguCrushV2.7 (regen target = 5, kills "4/5 + Full!" bug)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x4F82066ae2924B1EAd467A22C79FD1606718193E
  upgrade tx       0xa7e65fdf5a52e25497aff7c91b68b4816f9e3d2d75f3f9ac223b8047b531d07e
  block            62748278
  changes — single constant flip:
    REGEN_CAP_REGULAR  3 → 5
  Pre-V2.7 the HUD could show 4 pink + 1 ice with "Full!" underneath
  because regen stopped at 3 even when the row had unfilled slots.
  Now regen fills every regular slot to 5 and "Full!" only paints
  when total visible hearts (regular + ice) reach 5. Pure-constant
  change, no storage-layout impact.

2026-05-18  PenguCrushV2.6 (single-tx Next button)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x13586C19fb0c441EBC6CA38514ef4c4297Ae0274
  upgrade tx       0x0b18335a925a3063f290980b08151ac56182f78b5552d6d49ddc48a86f361b35
  block            62745044
  changes:
    + submitAndStartNext(LevelJournal, bytes sig, uint16 nextLevel)
      Fuses submitLevelValidated + startLevel into a single atomic tx.
      Same body as the two separate calls but in one external entry.
      Reverts atomically — if the next-level start fails (e.g. NoLives),
      the submit reverts too. Backs the level popup's Next + Replay
      buttons. No storage-layout change.
  selector: 0xcc50edd2 (verified live on impl bytecode).
  GAMEPLAY_METHODS session-key allowlist updated to include the new
  selector — existing session keys from before this commit must be
  re-issued (sign out → sign in) or they fall back to wallet prompt.

2026-05-18  PenguCrushV2.5 (hard cap on regular lives = 5)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x6C321B967fbd6aB80bFa0B8106062141d9396a88
  upgrade tx       0xcc0f73cb2e361bd2f59d8500f5e2b5d7fe268269e1ebfaffddf0de855b63b3db
  block            62742670
  changes — single constant flip:
    MAX_REGULAR_LIVES  10 → 5
  Reverted the V2.4 "stack up to 10" decision after product call:
  total HUD slots are 5, anything past 5 wastes. Existing wallets
  that ran past 5 during the V2.4 window keep their extra count on
  chain (not auto-rebased) and burn down as the player consumes lives.

2026-05-18  PenguCrushV2.4 (lives stack to 10, regen still 3)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x857d1DA040c501133782f68ca071296F1c47d522
  upgrade tx       0x1151cc6c7590e2954b35ca4b39ea8ef87ed007bc8b6c1a31a450ad199a595ac0
  block            62732696
  changes:
    MAX_REGULAR_LIVES  3 → 10  (purchases stack)
    REGEN_CAP_REGULAR  new = 3 (regen ceiling, unchanged behavior)
    Invariant: lastConsumedAt == 0 iff regular >= REGEN_CAP_REGULAR
  Buying a 5-pack of lives at full hearts no longer wasted the
  overflow. Superseded by V2.5 (UX call swung back to hard 5 cap).

2026-05-18  PenguCrushV2.3 (one-time starter pack)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x857d1DA040c501133782f68ca071296F1c47d522  ← also used by V2.4
  changes:
    + claimedStarterPack mapping(address => bool)
    + claimStarterPack() external — idempotent, grants 1 of each
      enabled Booster SKU. Permissionless + session-key safe.
    + StarterPackClaimed(address) event
  Frontend ensureStarterPack() promise-cached + auto-fires after
  first sign-in. Replaced the lying "starter pack via localStorage"
  pattern that had no chain backing.

2026-05-18  PenguCrushV2.2 (validation + score saturation)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0x0dB9cB1Aa682f50EaF1b6820f39210336c834f01
  upgrade tx       0x75e3819b867b801fc37977bd2c467dffad33d74d7f2c48d827d937174d8a6ea9
  abscan verified  ID 12783
  validatorRelayer set tx 0x38c8c6876f274f4643842524f46ec9c88d55c6b468202b5acb0302f2290b18d5
                   (same EOA as priceRelayer + wheelRelayer for now)
  changes — audit fixes:
    #10 New submitLevelValidated(j, sig) function. EIP-712 Validation
        typehash: keccak256("Validation(address player,bytes32 journalHash)")
        Validator (Supabase edge fn pengu-validate-level) bounds-checks
        the journal against per-level moves + score caps + recomputed
        stars threshold, then signs. Contract verifies sig recovers to
        validatorRelayer and emits LevelValidated(player, level, hash).
        Off-chain leaderboards filter on the new event for cheat-resistant
        rankings. Direct submitLevel still works (degrades gracefully if
        validator unreachable) but doesn't get the validated marker.
    #14 totalScore math switched from `a - b + c` (reverts on uint32
        overflow) to saturating add capping at uint32.max. Elite players
        who hit the cap stop accumulating but can keep improving their
        best-results for individual levels.
  storage layout: appended `address validatorRelayer` (1 slot), shrunk
    __gap[38] → __gap[37]. Manual layout review documented in
    deploy/upgrade-pengucrush-v2.cjs; passed unsafeSkipStorageCheck after.
  new edge function: pengu-validate-level (verify_jwt=false; chain
    signature is the trust boundary).

2026-05-18  PenguCrushV2.1 (audit-hardening upgrade)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896 (unchanged)
  new impl         0xe45A09e7B5816a16f6DA0cAd18c265DBFa174B91
  upgrade tx       0xf70ba8abef3c39bab5be1ab10d86d49f49c49f42dd93bb03377113f477cea4d0
  abscan verified  ID 12782
  changes — audit fixes:
    #8  startLevel now stamps levelStartedAt; submitLevel reverts if 0 ←
        closes infinite-plays exploit (life consumption mandatory)
    #9  MAX_SHARDS_PER_SUBMIT = 16; per-shard maxBalance set via admin:
          shard.necklace → 9999 (tx 0x22bb1e58…87b29)
          shard.crown    → 5000 (tx 0x2052efe1…5ebf)
          shard.plooshie → 2500 (tx 0x749a8eae…7ba6b)
    #11 Pyth state vars + setPythConfig retained but now explicitly
        documented as DEAD CODE (no read site in _verifyShopQuote)
    #13 ETH shop fns now require msg.value == q.amount (exact);
        overpayment reverts with ExactPaymentRequired instead of
        silently transferring excess to treasury
    #15 Wheel nonces moved to usedWheelNonces map; usedNonces is now
        shop-only (no collision between relayers)
    #16 LevelSubmitted event gains bytes32 journalHash so indexers can
        detect calldata rewrites without decoding the struct
  storage layout — manually verified compatible, OZ plugin's auto-check
    rejected gap-shrink-with-mappings pattern so passed
    unsafeSkipStorageCheck after review (see upgrade script comments)

2026-05-18  PenguCrushV2 (original deploy)
  proxy            0x06aCb91c46aD1359825560B19A9556118Aeb1896
  implementation   0xa742dd48E82B970770A557f89f062769B5A764B9
  owner            0xDdbbc73A42814c42943095cc7F51762fb038BC9A
  treasury         0xDdbbc73A42814c42943095cc7F51762fb038BC9A
  priceRelayer     0xDdbbc73A42814c42943095cc7F51762fb038BC9A
  wheelRelayer     0xDdbbc73A42814c42943095cc7F51762fb038BC9A
  usdc             0x84A71ccD554Cc1b02749b35d22F684CC8ec987e1
  pyth             0x8739d5024B5143278E2b15Bd9e7C26f6CEc658F1 (guard disabled)
  network          abstractMainnet (chainId 2741)
  post-deploy verified via RPC:
    paused=false  shopPaused=false  gameplayPaused=false  maxLevel=20
    items seeded = 13  wheel slots seeded = 6
  Deploy issues fixed mid-flight:
    ✗ OZ ReentrancyGuard trips upgrades-plugin safety validator
      (constructor not allowed).
      ✓ FIX: inlined a minimal nonReentrant modifier using uint256 status flag
        + custom Reentrant() error. Removed import + inheritance.

When something gets deployed, append:
  YYYY-MM-DD  contract  proxy_address  impl_address  tx_hash  network

2026-05-18 — Security hardening pass 1 (audit fixes #1-#7)
  ▸ Fix #1 — supabase/config.toml project_id corrected to
    saftqlwxmdqxzfuwdgtu (was abandoned project ref).
  ▸ Fix #2 — rpc_upsert_player_progress: wallet regex + level/score/
    stars/moves bounds + EXECUTE revoked from anon/authenticated.
    pengu_players.wallet_address constraint check added.
    Frontend saveLevelResult now a graceful no-op (returns null);
    leaderboard mirror stays empty until a signed-write edge function
    lands. Follow-up: pivot leaderboard reads to on-chain getPlayers +
    getLeaderboardBatch.
  ▸ Fix #3 — pengu_game_snapshots: wallet regex constraint + INSERT
    trigger caps each wallet at 30 active snapshots. RLS policies
    rewritten to require wallet format match.
  ▸ Fix #4 — entry.js buildRow now uses document.createElement +
    textContent instead of innerHTML += interpolation. shortAddr
    validates 0x+40hex before slicing; non-matching returns '???'.
    Also re-templated the placeholder rows to use createElement.
  ▸ Fix #5 — CRON_SECRET (32-byte hex) inserted into vault.secrets;
    pg_cron job pengu-regen-sweep-hourly reissued to pass header
    `x-cron-secret`. Edge function v4 enforces constant-time compare
    against the secret; returns 401 without it (verified live).
  ▸ Fix #6 — getEthUsdPrice now FAILS CLOSED. New EthUsdUnavailable
    error; index.ts returns HTTP 503 retryAfterSec:30 instead of
    silently quoting at $3000. Opt-in flag
    ETH_USD_PRICE_FALLBACK_ALLOWED=true + ETH_USD_FALLBACK still
    available for dev environments.
  ▸ Fix #7 — Content-Security-Policy meta added to index.html:
    script-src self + googletagmanager + gstatic, style-src self +
    fonts.googleapis, img-src self + data + blob + https, connect-src
    self + *.supabase.co + Abstract RPC + GA + Privy. object-src none,
    base-uri self, form-action self.
  ▸ Live verification:
    - quote-price returns valid signed quote for ETH purchase
    - regen-sweep without secret → 401 unauthorized
    - regen-sweep with bad secret → 401 unauthorized
    - regen-sweep with correct secret → 200 {swept:0}
    - direct supabase.rpc('rpc_upsert_player_progress',...) from anon
      → 42501 permission denied
  ▸ Migration applied: 20260518111500_v2_security_hardening
    (local file omits the CRON_SECRET value; insert separately on
    fresh-project replay).
  ▸ Out-of-scope this pass (contract upgrade required, queued for V2.1):
    audit findings #8-#17 — startLevel-required precondition, shard
    array cap, server-side stars validation, Pyth wiring or removal,
    ETH overpay refund, totalScore uint64, journal hash in event,
    per-relayer nonce separation, owner→multisig transfer (declined
    by user this session).

2026-05-18 — Session 2 (V2.3 → V2.7 + frontend hardening)
  Theme: chain is truth (no more client-side lies), single-tx UX, HUD
  caps at 5 hearts, RPC routes through a dedicated endpoint.

  Bugs found and fixed:
    ▸ Shop "+5 ✓" optimistic label even when chain tx silently failed.
      → chainWrite() now throws (no more silent null); button shows
      "Sign tx…" → "✓ Confirmed" only after a real receipt.
    ▸ Wallet client null after every page reload ("wallet client missing —
      reconnect AGW" on shop / claim).
      → src/agw.js ensureWalletClient(): silent reconnect on every page
      load using Privy's stored cross-app connection, no popup.
    ▸ Wheel animated to a client-picked slot BEFORE the chain tx fired.
      Player saw a "winning" spin followed by a revert if gas was low.
      → src/map.js spinDailyWheel: fire chain first, decode landing slot
      from DailySpin event in receipt, animate to that slot. Surfaces
      actionable "fund your AGW wallet" message on insufficient balance.
    ▸ submitLevel was fire-and-forget on popup-show race. Page navigated
      with the tx still pending; chain never recorded the run; next level
      stayed locked; leaderboard missed the score; boosters used in-game
      never debited on chain.
      → V2.6 submitAndStartNext fused contract function. Map button is
      pure exit; Next/Replay each fire ONE atomic tx that settles the
      current run + starts the destination level.
    ▸ Boosters used in-game still showed on the next level (because
      submit never landed — same root cause as above; fix is the same).
    ▸ Pricing bug: edge function treated $2.98 booster + $2.99 lives as
      PER-UNIT prices and multiplied by qty. Clicking the "$2.98" 4-pack
      actually charged $11.92 in ETH; the 5-pack of lives charged $14.95.
      → pengu-quote-price v5: replaced SKU_UNIT_USD_MICROS with
      SKU_BUNDLES {size, priceMicros}. Server clamps qty to canonical
      bundle size so a tampered request can't drain bundles for one
      bundle's price.
    ▸ Lives HUD "Full!" with 4/5 hearts visible. Regen capped at 3 even
      though row had unfilled slots.
      → V2.7 REGEN_CAP_REGULAR 3 → 5. Frontend LIVES_MAX → 5. HUD
      always paints exactly 5 slots; regulars displace ice positions
      when stacked above the regen cap.
    ▸ Lives count silently truncated back to 3 on every HUD render tick
      (applyLifeRegenToState clamping s.lives at LIVES_MAX every second).
      → Removed clamp inside the "at regen cap" branch — s.lives just
      doesn't regen further, doesn't get rewritten.
    ▸ "Next" took user to homepage / map instead of level 2.
      Root cause: chain submit never landed, gate blocked level 2.
      → submitAndStartNext + chain-progress hydrate fixed both layers.
    ▸ Leaderboard always empty after wins.
      Root cause: pengu_leaderboard mirror table has no signed writer
      (anon revoked in audit) so it never populated.
      → fetchLeaderboard now reads on chain via getPlayerCount +
      getPlayers + getLeaderboardBatch. Chain is the source.
    ▸ Treasury revenue going to deployer EOA, not the multisig.
      → setTreasury rotated to the 2/2 Safe multisig
      0xBCaFE23d6D072d65d87E138AfFefc61342D73d0b in this session's
      earlier work; verified each shop fn forwards to treasury via
      _forwardEth on every buy. Treasury balance at end of session:
      0.013495 ETH (~$28.41).
    ▸ "+5 ✓" / "x4 necklace" lies in the pre-game popup were showing
      lifetime shard totals, not per-level. User reading "Level 5 has
      dropped 4 necklaces" when really they came from other levels.
      → New per-level shard ledger in localStorage
      (pengucrush_level_shards_v1) keyed by {wallet}:{level}. Pre-game
      popup renders getLevelShards(level).
    ▸ V2.6 push shipped without regenerating contracts/PenguCrushABI.json.
      submitAndStartNext was on chain but the frontend ABI didn't know
      the selector — viem threw AbiFunctionNotFoundError on Next click.
      → Regenerated ABI from artifacts-zk/...json in the V2.7 commit.
      Added "regen ABI in the SAME commit as contract bumps" to the
      pre-push checklist permanently.
    ▸ Variable rename leftover: renderLivesHud's destructure went
      `total → visibleTotal` but a stray `total` ref in the popupPlay
      enable/disable block threw ReferenceError every HUD tick (1Hz
      setInterval). Uncaught exception inside the first renderLivesHud
      call aborted initMap, which silently killed renderNodes — the
      map drew without level markers.
      → Replaced with visibleTotal. Added "grep for stale refs after
      every refactor" to the pre-push checklist permanently.

  Other changes:
    + Alchemy RPC endpoint wired via VITE_ABSTRACT_RPC_URL env var.
      Reads route off the shared public RPC to a dedicated endpoint
      so hydrate bursts + receipt waits + leaderboard fetches stop
      hitting rate-limit walls. Key swapped once mid-session.
      Stored locally in .env.local (gitignored) and Vercel prod env
      only. Never written into records / commits / source.
    + Inventory popup now hides items with count 0 — "No items yet"
      empty-state instead of a wall of ×0 slots.
    + Pre-game popup hydrates progress from chain on every map load
      (getBestResult for each of the 20 nodes in parallel) so a fresh
      device / cleared cache / new session shows real on-chain stars
      instead of all-locked.
    + cancelCrushPass button now actually calls the chain (was local-
      only; next hydrate silently undid it).
    + hydrateFromChain gets a 400ms one-shot retry when all three
      reads return null (transient RPC blips no longer leave HUD stale).
    + Inventory cloud-sync (pengu_players / pengu_boosters anon upserts)
      removed — anon writes were revoked in the audit so the upserts
      were 401-spamming the network tab for zero benefit. Chain is the
      only authoritative store now.
    + pengu_tx_log mirror table: every chain write now logs to
      pengu_tx_log {wallet, tx_type, status, tx_hash, block_number,
      details, error}. Per-wallet cap = 10000 rows. Lets admin query
      "what did wallet X do today" without crawling RPC.

  Live verification (opera-browser-cli on prod):
    - All ABI strings present in deployed bundle (submitAndStartNext,
      getBestResult, etc. — 21 functions confirmed)
    - 0 broken `total <=` regex matches in deployed map bundle
    - Alchemy endpoint returns eth_chainId 0xab5 (2741) in 235ms
    - All 4 edge functions OPTIONS 200; pengu-quote-price returns
      valid signed quote for ETH purchase
    - Chain reads: MAX_REGULAR_LIVES=5, REGEN_CAP_REGULAR=5,
      MAX_FROZEN_LIVES=2, maxLevel=20, getPlayerCount=1
    - getLeaderboardBatch returns valid PlayerStats; one player with
      totalStars=5, totalScore=18313, highestLevel=4, played=6, won=6

  Pre-push checklist established (saved to ~/.claude memory):
    1. npm run build (necessary but not sufficient)
    2. grep for stale refs across whole file after any rename
    3. Regenerate PenguCrushABI.json in the SAME commit as any
       contract change
    4. opera-browser-cli open prod URL → console scan → bundle
       grep for the expected symbol strings
    5. Surface auth-gated paths the agent can't reach so the user
       knows what to verify post-deploy

═══════════════════════════════════════════════════════════════════════════════
5. OUTSTANDING QUESTIONS / TODO
═══════════════════════════════════════════════════════════════════════════════

[x] Confirm Abstract mainnet USDC contract address  → 0x84A71ccD…87e1
[x] Confirm AGW session-key API surface             → AGW validator
                                                       0x34ca1501…d7d081
[x] Mid-game state snapshot                         → localStorage + Supabase
                                                       + on-chain LevelCheckpoint
                                                       every 5 moves
[ ] Mid-game RESUME UI on level open (snapshot save works, restore deferred)
[x] Split deployer/treasury/relayer EOAs            → relayer rotated to
                                                       0xeF95BD4e…CF31 (separate
                                                       lower-privilege EOA).
                                                       Deployer + treasury still
                                                       collapsed for now.
[x] Fund relayer                                    → bridge tx mis-routed to
                                                       deployer; forwarded
                                                       0.003 ETH deployer→relayer
                                                       tx 0xc96d7b7b…137c1
[ ] Rotate relayer key (transcript exposure during bring-up)
[x] Abscan source verification                       → both proxy + impl
                                                       verified (IDs 12781,
                                                       12780). Open
                                                       abscan.org/address/
                                                       0x06aCb91c…1896 to see
                                                       readable Read/Write tabs.
[x] Google Analytics (gtag.js)                       → installed with
                                                       G-R5T4Y4B629 + custom
                                                       events for every
                                                       in-app action via
                                                       src/analytics.js
[x] Wheel "Ice Boost" semantics                     → random pool resolved on
                                                       chain via setRandomPool;
                                                       wheel relayer rolls slot
                                                       index, contract picks
                                                       random member from pool
[x] Supabase MCP auth                               → fresh V2 project under
                                                       Free org (legacy abandoned)
[x] Set RELAYER_PRIVATE_KEY Supabase secret         → stored in vault.secrets,
                                                       read via get_vault_secret
[ ] Set LIFE_REGEN_RELAYER_PK secret (if/when life-regen sweeper needs its own key)
[x] Test full flow on production after push         → opera-browser-cli verified
                                                       bundle hashes, ABI strings,
                                                       chain reads, edge fns, RPC
                                                       (auth-gated paths verified
                                                       end-to-end by user with
                                                       wallet)
[ ] Verify Abscan source verification (needs ABSCAN_API_KEY)
[x] Pivot leaderboard reads to on-chain getPlayers + getLeaderboardBatch
                                                    → fetchLeaderboard rewritten
                                                       in src/supabase.js
[x] Treasury rotation to multisig                   → setTreasury to
                                                       0xBCaFE23d…d73d0b (2/2 Safe)
[x] Hydrate progress from chain on map load         → hydrateProgressFromChain()
                                                       reads getBestResult for
                                                       every node in parallel
[x] Single-tx Next button                           → V2.6 submitAndStartNext
[x] Dedicated RPC endpoint                          → VITE_ABSTRACT_RPC_URL
                                                       (Alchemy) in Vercel +
                                                       .env.local
[x] Hide 0-count inventory items                    → renderInventoryGrid skips
                                                       zero rows, shows "No items
                                                       yet" if all empty
[x] Per-level shard tally                           → pengucrush_level_shards_v1
                                                       in localStorage, keyed by
                                                       {wallet}:{level}
[ ] Add shard display element to end-of-level popup HTML
    (#levelPopup currently has no shard slot; per-run shards are tracked
     in the levelShards JS module but not rendered)
[ ] Signed-write edge function for pengu_leaderboard mirror table
    (currently empty because anon writes were revoked; leaderboard reads
     are on-chain instead, which works but pays a per-load RPC cost)
[ ] Resume UI for mid-game snapshots (save flow works, restore deferred)
[ ] Rotate relayer key (still pending — transcript exposure during V2 bring-up)
[ ] Sweep stuck "submitted" tx_log rows older than 1h to either success
    (if receipt now exists on chain) or "abandoned"
[ ] Decide on owner→multisig contract-ownership transfer
    (user explicitly declined this session; treasury-only is enough for now,
     leaving owner = deployer EOA for admin actions)
